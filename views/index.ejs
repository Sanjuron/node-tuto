<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head.ejs') %> 

<body>
        <!-- pour importer les partials, signe - et chemin relatif -->
        <%- include('./partials/nav.ejs') %> 


    <div class="blogs content">
        <h2>All blogs</h2>

        <% if (blogs.length > 0){ %> 
            <% blogs.forEach(blog => { %>
                <h3 class='title'><%= blog.title %></h3>
                <p class='snippet'><%= blog.snippet %></p>
            <% }) %>
        <% } else { %> 
            <p>Pas de blogs à montrer...</p>
        <% } %> 
    </div>
    <%- include('./partials/footer.ejs') %> 

</body>
</html>